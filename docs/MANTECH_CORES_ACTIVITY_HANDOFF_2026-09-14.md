Review and apply the relevant Turtle activity fixes to our ManTech CMaNGOS Classic/TBC/WotLK baselines, using each core's actual scheduling and ownership contracts. Do not change production or reduce bot activity to hide a defect.

Read D:\WoW Emu\ManTech\ManTech Turtle\docs\TURTLE_ACTIVITY_FIXES_2026-09-14.md, TURTLE_FULL_ACTIVITY_FIXES_2026-09-14.md, and the September14 build84/85 sections of docs/CORE_SYSTEMS_GUIDE.md. The most important discovery: our Turtle minimal-update throttle reused the real AI action timer. Admitting GUID-staggered full updates did not guarantee decisions past that timer. The retained build83 hooks reproduce 5,369/10,000 simulated idle bots getting zero full decisions in five minutes with the live passive-delay setting; corrected hooks get zero starved bots while preserving an actual five-second action wait. See tests/architecture/run_background_ai_test.py and fixtures/Build83BackgroundHooks.inc.

Trace how each CMaNGOS core schedules full/minimal AI and how the shared module decrements/yields its timer. Check whether this defect actually exists there before porting. Separate scheduler throttling from real action/spell deadlines where necessary; preserve deferred elapsed accounting, native reaction behavior, player/group responsiveness and transfer/logout lifetime guards. Measure decisions and progress, not merely eligibility or admitted callback counts.

Also evaluate constructor-only packet handler lookup before queue locking; retention of untouched packets on callback exceptions; native count-only mailbox observation to prevent empty-mail world continuations; and soft-deleted mail row ownership. Verify existing equivalents before introducing new code. The moving/casting active-lane correction and NPC80856 trainer data are Turtle-specific unless your source proves otherwise.

Include the earlier chat fixes in the comparison: the no-human-audience LLM branch must terminate without accidentally falling through into canned bot-to-bot replies; cheap recipient admission should precede expensive strategy/name lookups; synchronous sender classification must retain nesting/lifetime guards; and locale or wrapper-allocation optimizations must preserve native comparisons and player lookup lifetimes. The older low-tick population acceptance did not establish full independent bot decisions, so do not repeat that inference.

Build85 also fixes unnecessary serialized travel admission: non-PREPARE choose attempts and existing active/no-destination flags should be rejected on the AI owner before queuing group/world work. Preserve derived refresh/reset/group selection rules and their native alternatives. Never use asynchronously changing future readiness to decide that an action may execute on the map thread. This issue matters only if your core uses comparable ownership handoffs; retain existing equivalents where they already work. See turtle_travel_admission_regression.py and the build85 guide section.

Build86 adjusts the existing serialized queue's service allowance to clamp(tick/4,8,32)ms while retaining1024 pending entries,256 callbacks per pass and all ownership/lifetime guards. A fixed8ms allowance was losing service capacity per second as world ticks lengthened. Evaluate the analogous service policy only where your CMaNGOS core has that bottleneck; do not copy these numbers blindly. Single callbacks can exceed a soft budget, so retain overrun diagnostics and measure actual queues, latency and native work costs. The actual queue regression uses a deterministic clock to test those limits.

Use the new bounded combat totals and per-bot progress approach if diagnostics have the same blind spots. Preserve genuine cast/action delays and avoid bypassing ownership with broad multithreading. Run focused native regressions and a sustained player-present workload. Report exact changes, measured progress/ticks/queues and unverified limits; do not equate low CPU usage with healthy bot behavior.
